var lg = lg || {},
	cardWidth = 70,
	cardHeight = 102,
	numofPlayer = 4,
	rowWidth = $(window).width() * 0.8,
	rowHeight = ($(window).height() - cardHeight * 2 - 53) * 0.8,
	drawCardSpeed = 100,
	firstPlayer = 3,
	cardsLeft = 6;

